# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
__pycache__/

# never commit private seeds with real learner data
supabase/seed-private-real.sql

# authoring/source material — keep out of the public repo
theory references/

# Her handwritten notes scans live here for digest sessions - NEVER commit (public repo)
*.pdf
tools/_out/
# shoot.py's PNG output (its own header names these two folders; they were
# never git-ignored, so a `git add -A` after a crop run would have committed
# a few hundred screenshots into a public repo). Added 2026-08-23.
tools/diags/
tools/shots/
# shoot_walk.py's output, same reason (session G3, 2026-08-23): one crop per
# memo step per card is a lot of screenshots, and this repo is public.
tools/diags-walk/
tools/shots-walk/
# funfun phone-walk screenshots (session 2, 2026-08-23) — public repo, keep out
sessions/funfun-walk/
